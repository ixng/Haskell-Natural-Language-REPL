# Haskell-Natural-Language-REPL
Built a read-eval-print loop in Haskell that parses English-language queries into a typed request algebra, composing small parser combinators into a grammar covering dates, arithmetic expressions and stateful recall.
